# hex-aura-clock
A real-time digital clock that converts the current time into a live-changing HEX color and displays it with smooth background transitions. A responsive clock that turns time into color — updates every second with a dynamic HEX background.
